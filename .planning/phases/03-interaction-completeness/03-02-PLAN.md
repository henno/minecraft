---
phase: 03-interaction-completeness
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
  - 03-01
files_modified:
  - src/world/World.ts
  - src/core/Game.ts
  - src/meshing/CulledMesher.ts
  - src/world/BlockRegistry.ts
autonomous: false
requirements:
  - BLCK-01
  - BLCK-02
  - BLCK-04

must_haves:
  truths:
    - "Left-click removes the targeted block and the visual gap appears immediately"
    - "Right-click places a valid block in the adjacent voxel of the targeted face"
    - "Editing a voxel on a chunk border dirties every affected chunk so seams update immediately"
    - "Block placement never overwrites the player's occupied space"
  artifacts:
    - path: "src/world/World.ts"
      provides: "Edit operations and dirty propagation for local and neighbour chunks"
    - path: "src/core/Game.ts"
      provides: "Integration of target selection, placeable block choice, and remesh requests"
    - path: "src/world/BlockRegistry.ts"
      provides: "Placeable block rules and any excluded/non-placeable definitions"
  key_links:
    - from: "src/core/Game.ts"
      to: "src/world/World.ts"
      via: "break/place actions route through world-owned block writes"
      pattern: "setBlock|markDirty|remesh"
    - from: "src/world/World.ts"
      to: "src/meshing/CulledMesher.ts"
      via: "dirty chunk selection includes border neighbours after edits"
      pattern: "needsMesh"
---

<objective>
Turn the targeting scaffold into a complete editing loop: break blocks, place valid blocks, and remesh all affected chunks immediately without visual seams or collision exploits.
</objective>

<context>
@.planning/phases/03-interaction-completeness/03-CONTEXT.md
@.planning/phases/03-interaction-completeness/03-01-PLAN.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend world editing to dirty border neighbours</name>
  <files>src/world/World.ts</files>
  <action>
    Upgrade the block-write path so edits on chunk edges mark the touched chunk plus any adjacent chunk whose exposed faces changed, while keeping `World` as the only voxel-data owner.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add break/place rules in the game loop</name>
  <files>src/core/Game.ts, src/world/BlockRegistry.ts</files>
  <action>
    Consume the targeting result and input edges to remove blocks, place selected block types, reject placement into the player's collision space, and keep the selected block type accessible via a minimal UI/control scheme.
  </action>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the edit loop in browser</name>
  <action>Run `npm run dev`, lock the pointer, break several blocks, place several different block types, and specifically test edits on chunk borders and near the player body.</action>
  <resume-signal>
    Type "approved" if break/place/remesh behavior looks correct, or describe the visible issue (e.g. border seam, delayed remesh, placement inside player, wrong target).
  </resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- Browser check: break and place are both immediate; border edits update adjacent chunks with no stale faces
</verification>

<output>
Create `.planning/phases/03-interaction-completeness/03-02-SUMMARY.md` with edit rules, border-dirtying logic, placement restrictions, and browser verification notes.
</output>
