---
phase: 03-interaction-completeness
plan: 01
type: execute
wave: 1
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/core/Game.ts
  - src/player/Player.ts
  - src/input/InputManager.ts
  - src/world/World.ts
  - src/types.ts
autonomous: true
requirements:
  - BLCK-01
  - BLCK-02

must_haves:
  truths:
    - "The engine can identify the exact targeted block and the adjacent placement face from the player's camera each frame or input event"
    - "Left-click and right-click interaction plumbing works without opening the browser context menu on the canvas"
    - "Targeting logic reads world voxels authoritatively, not rendered mesh triangles"
    - "Interaction range is finite and tuned for normal first-person play"
  artifacts:
    - path: "src/player/Player.ts"
      provides: "Camera-origin targeting query and current interaction state"
    - path: "src/core/Game.ts"
      provides: "Input-to-interaction wiring for break/place actions"
    - path: "src/world/World.ts"
      provides: "Shared helper surface for reading target voxels safely"
  key_links:
    - from: "src/core/Game.ts"
      to: "src/player/Player.ts"
      via: "current targeted block / face queried from player state"
      pattern: "target|raycast|pick"
    - from: "src/core/Game.ts"
      to: "src/input/InputManager.ts"
      via: "edge-triggered mouse actions for break/place"
      pattern: "mouse|primary|secondary|contextmenu"
---

<objective>
Add the targeting foundation for Phase 3: a deterministic block-picking path from the first-person camera plus safe mouse interaction wiring for break/place input.
</objective>

<context>
@.planning/phases/03-interaction-completeness/03-CONTEXT.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/phases/02-world-player/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement voxel targeting query</name>
  <files>src/player/Player.ts, src/world/World.ts, src/types.ts</files>
  <action>
    Add a camera-based voxel traversal helper that returns the targeted solid voxel, the hit face normal, and the adjacent placement position within a fixed reach distance.
  </action>
</task>

<task type="auto">
  <name>Task 2: Wire break/place input safely</name>
  <files>src/input/InputManager.ts, src/core/Game.ts</files>
  <action>
    Capture primary/secondary mouse actions while pointer lock is active, suppress the browser context menu on the canvas, and expose edge-triggered interaction events that Phase 3 can consume without repeat-spam bugs.
  </action>
</task>

<task type="auto">
  <name>Task 3: Expose minimal targeting feedback</name>
  <files>src/core/Game.ts</files>
  <action>
    Surface the currently targeted block in the existing HUD/crosshair flow or via a lightweight visual indicator so break/place verification is practical.
  </action>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- Browser check: aiming at terrain returns stable target selection at close range; right-click on canvas does not open the browser menu
</verification>

<output>
Create `.planning/phases/03-interaction-completeness/03-01-SUMMARY.md` with raycast approach, interaction range, input edge-trigger rules, and any target-feedback affordance added.
</output>
