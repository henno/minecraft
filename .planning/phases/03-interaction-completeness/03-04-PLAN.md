---
phase: 03-interaction-completeness
plan: 04
type: execute
wave: 4
depends_on:
  - 02-04
  - 03-03
files_modified:
  - src/core/Game.ts
  - src/core/Renderer.ts
  - src/world/World.ts
  - src/meshing/CulledMesher.ts
autonomous: false
requirements:
  - REND-04

must_haves:
  truths:
    - "The engine demonstrates 60fps at a reasonable render distance (target 8+ chunks) on the validation machine under recorded conditions"
    - "Any tuning preserves the established architecture: chunk-batched rendering, worker terrain generation, and explicit chunk disposal"
    - "Performance validation is written down with enough detail to repeat later"
    - "No new console errors or runaway memory behavior appear during the performance pass"
  artifacts:
    - path: "src/core/Game.ts"
      provides: "Performance counters, pacing limits, or runtime knobs used for validation"
    - path: "src/world/World.ts"
      provides: "Streaming/request tuning if needed for stable frame pacing"
    - path: "src/meshing/CulledMesher.ts"
      provides: "Any low-risk hot-path improvements made during tuning"
  key_links:
    - from: "src/core/Game.ts"
      to: "src/world/World.ts"
      via: "render-distance or mesh-update pacing tuned against streaming workload"
      pattern: "LOAD_RADIUS|UNLOAD_RADIUS|MAX_MESH_UPDATES_PER_FRAME"
    - from: "src/core/Renderer.ts"
      to: "src/core/Game.ts"
      via: "fog/render distance and validation settings remain aligned"
      pattern: "fog|renderDistance|stats"
---

<objective>
Close v1 with a measured performance pass: tune low-risk bottlenecks if necessary, then verify and document that the engine meets the 60fps target at a reasonable render distance.
</objective>

<context>
@.planning/phases/03-interaction-completeness/03-CONTEXT.md
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure and tune low-risk performance hotspots</name>
  <files>src/core/Game.ts, src/core/Renderer.ts, src/world/World.ts, src/meshing/CulledMesher.ts</files>
  <action>
    Use the existing Stats overlay and browser profiling to identify the biggest low-risk bottlenecks, then apply small targeted tuning such as pacing, radius adjustment, or avoidable allocation cleanup without changing the fundamental meshing strategy.
  </action>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the final 60fps validation</name>
  <action>Run `npm run dev` or a production preview, test at a clearly stated render distance (target 8+ chunks), record the machine/context, and verify whether the engine sustains roughly 60fps while exploring.</action>
  <resume-signal>
    Type "approved" if the target is met, or report the measured bottleneck / framerate so the plan can continue with focused tuning.
  </resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- Browser/perf check: validation notes record FPS behavior, render distance, scene conditions, and whether the 60fps target was met
</verification>

<success_criteria>
Phase 3 is complete when all success criteria from `ROADMAP.md` are true:
1. Left-click removes a block and the gap appears immediately
2. Right-click places the selected block on the adjacent face
3. Water is visibly translucent with submerged terrain visible through it
4. The engine maintains 60fps at a reasonable render distance on the validation machine
</success_criteria>

<output>
Create `.planning/phases/03-interaction-completeness/03-04-SUMMARY.md` with tuning changes, validation setup, measured results, and any remaining caveats.
</output>
