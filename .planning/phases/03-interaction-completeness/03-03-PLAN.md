---
phase: 03-interaction-completeness
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 03-02
files_modified:
  - src/meshing/CulledMesher.ts
  - src/rendering/ChunkMaterial.ts
  - src/core/Game.ts
  - src/world/BlockRegistry.ts
  - src/rendering/TextureAtlas.ts
autonomous: false
requirements:
  - BLCK-03
  - REND-05

must_haves:
  truths:
    - "Water renders with visible translucency and blue tint rather than as an opaque terrain block"
    - "Terrain beneath or behind water remains visible through it from common viewing angles"
    - "The placeable block palette reaches the roadmap target of 5-10 recognizable block types"
    - "Opaque terrain rendering remains correct and does not inherit transparent-material artifacts"
  artifacts:
    - path: "src/meshing/CulledMesher.ts"
      provides: "Water/transparent geometry separation or equivalent meshing path"
    - path: "src/rendering/ChunkMaterial.ts"
      provides: "Opaque and transparent chunk material configuration"
    - path: "src/world/BlockRegistry.ts"
      provides: "Completed placeable block palette and transparent-block metadata"
  key_links:
    - from: "src/core/Game.ts"
      to: "src/meshing/CulledMesher.ts"
      via: "chunk rebuild path uploads both opaque and water-render data"
      pattern: "buildChunkMesh|water|transparent"
    - from: "src/rendering/ChunkMaterial.ts"
      to: "src/rendering/TextureAtlas.ts"
      via: "shared atlas sampling remains consistent across opaque and transparent passes"
      pattern: "atlas|texture"
---

<objective>
Finish the visual completeness work for v1 by making water read as translucent and by completing the placeable block palette without regressing the opaque terrain path.
</objective>

<context>
@.planning/phases/03-interaction-completeness/03-CONTEXT.md
@.planning/phases/02-world-player/02-01-PLAN.md
@.planning/research/PITFALLS.md
@.planning/research/FEATURES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Separate water from opaque terrain rendering</name>
  <files>src/meshing/CulledMesher.ts, src/rendering/ChunkMaterial.ts, src/core/Game.ts</files>
  <action>
    Introduce a dedicated transparent rendering path for water geometry/material so water can use tint and opacity while opaque chunk rendering keeps stable depth behavior.
  </action>
</task>

<task type="auto">
  <name>Task 2: Complete the placeable block palette</name>
  <files>src/world/BlockRegistry.ts, src/rendering/TextureAtlas.ts, src/core/Game.ts</files>
  <action>
    Add the remaining recognizable v1 block types (for example wood and leaves) to the atlas/registry and make them reachable through the minimal block-selection UX.
  </action>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify water readability in browser</name>
  <action>Run `npm run dev`, place or observe water near terrain edges, and confirm the tint/transparency look intentional while submerged terrain remains visible.</action>
  <resume-signal>
    Type "approved" if water reads correctly, or describe the artifact (e.g. sorting issue, opaque water, depth popping, wrong tint).
  </resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- Browser check: water is visibly translucent and blue-tinted; at least 5 placeable block types are selectable and place correctly
</verification>

<output>
Create `.planning/phases/03-interaction-completeness/03-03-SUMMARY.md` with transparent-rendering approach, material settings, final block palette, and browser verification notes.
</output>
