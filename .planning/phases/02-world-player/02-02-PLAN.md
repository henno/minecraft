---
phase: 02-world-player
plan: 02
type: execute
wave: 2
depends_on:
  - 01-04
  - 02-01
files_modified:
  - src/input/InputManager.ts
  - src/player/PlayerCamera.ts
  - src/player/Player.ts
  - src/core/Game.ts
autonomous: true
requirements:
  - PLYR-01
  - PLYR-02

must_haves:
  truths:
    - "Clicking the canvas or overlay acquires pointer lock and enables mouse look"
    - "Mouse movement controls first-person yaw/pitch with pitch clamped to prevent flipping"
    - "WASD movement intent is relative to the camera facing direction"
    - "A visible centered crosshair and click-to-play hint are present when unlocked"
    - "Pressing Escape cleanly releases pointer lock without breaking camera state"
  artifacts:
    - path: "src/input/InputManager.ts"
      provides: "Keyboard and mouse delta state for the current frame"
    - path: "src/player/PlayerCamera.ts"
      provides: "First-person camera wrapper with pointer lock mouse look"
    - path: "src/player/Player.ts"
      provides: "Top-level player controller that converts input into desired movement"
  key_links:
    - from: "src/player/Player.ts"
      to: "src/input/InputManager.ts"
      via: "per-frame movement intent sampling"
      pattern: "InputManager"
    - from: "src/player/Player.ts"
      to: "src/player/PlayerCamera.ts"
      via: "yaw/pitch updates and camera transform sync"
      pattern: "PlayerCamera"
---

<objective>
Add the first-person exploration layer: browser-friendly pointer lock UX, mouse look, and camera-relative WASD intent, all integrated into the existing game loop.
</objective>

<context>
@.planning/phases/02-world-player/02-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add input manager and pointer lock HUD</name>
  <files>src/input/InputManager.ts, src/core/Game.ts</files>
  <action>
    Capture keyboard state, mouse deltas, pointer lock state, and prevent browser context leakage where relevant. Add a simple DOM overlay/crosshair so first-time users know to click to play.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add first-person camera wrapper</name>
  <files>src/player/PlayerCamera.ts</files>
  <action>
    Wrap the existing renderer camera with yaw/pitch state, clamped pitch, and camera transform updates driven by input deltas.
  </action>
</task>

<task type="auto">
  <name>Task 3: Add player controller shell</name>
  <files>src/player/Player.ts, src/core/Game.ts</files>
  <action>
    Introduce a `Player` class that owns camera + input integration and exposes movement intent to later physics work. The player should be wired into the frame loop but not yet responsible for gravity/collision.
  </action>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- Browser check: click enters pointer lock, mouse look works, `W` moves forward relative to camera yaw, Esc releases pointer lock
</verification>

<output>
Create `.planning/phases/02-world-player/02-02-SUMMARY.md` with pointer-lock UX details, camera sensitivity/clamp values, and any browser quirks handled.
</output>
