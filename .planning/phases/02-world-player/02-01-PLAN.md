---
phase: 02-world-player
plan: 01
type: execute
wave: 1
depends_on:
  - 01-03
  - 01-04
files_modified:
  - src/rendering/TextureAtlas.ts
  - src/rendering/ChunkMaterial.ts
  - src/world/BlockRegistry.ts
  - src/meshing/CulledMesher.ts
  - src/core/Game.ts
autonomous: true
requirements:
  - REND-01
  - REND-02
  - REND-03

must_haves:
  truths:
    - "Blocks render with distinct 16x16 pixel-art textures for at least GRASS, DIRT, STONE, SAND, and WATER-facing surfaces in the atlas"
    - "The atlas uses nearest-neighbour filtering so textures stay crisp, not blurry"
    - "Each block face receives the correct UV rect for its block type and face direction (e.g. grass top != grass side)"
    - "Hidden faces between adjacent solid blocks remain culled — Phase 1 performance architecture is preserved"
    - "Directional face shading is visible: top faces are brightest, side faces dimmer, bottom faces darkest"
  artifacts:
    - path: "src/rendering/TextureAtlas.ts"
      provides: "Runtime-generated texture atlas + UV lookup for block faces"
    - path: "src/rendering/ChunkMaterial.ts"
      provides: "Shared textured chunk material configured for crisp pixel art"
    - path: "src/meshing/CulledMesher.ts"
      provides: "Atlas-aware UV emission and per-face shade/color data"
  key_links:
    - from: "src/core/Game.ts"
      to: "src/rendering/ChunkMaterial.ts"
      via: "shared material for all opaque chunk meshes"
      pattern: "createChunkMaterial|ChunkMaterial"
    - from: "src/meshing/CulledMesher.ts"
      to: "src/world/BlockRegistry.ts"
      via: "face-specific UV lookup and/or face brightness rules"
      pattern: "getBlockDef|texture|uv"
---

<objective>
Upgrade the Phase 1 flat-color terrain into a recognisable Minecraft-style world by introducing an atlas-backed material, atlas-aware UV generation, and simple directional face shading.
</objective>

<context>
@.planning/phases/02-world-player/02-CONTEXT.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime texture atlas and block-face metadata</name>
  <files>src/rendering/TextureAtlas.ts, src/world/BlockRegistry.ts</files>
  <action>
    Create a runtime atlas generator that draws simple 16x16 pixel-art tiles into a canvas, adds padding between tiles, uploads a `THREE.CanvasTexture`, and exposes UV rect lookup by block face. Extend block definitions so faces can map to different atlas tiles (grass top / side / dirt bottom).
  </action>
</task>

<task type="auto">
  <name>Task 2: Emit textured shaded chunk geometry</name>
  <files>src/meshing/CulledMesher.ts</files>
  <action>
    Extend mesh buffers so each emitted face gets the correct atlas UVs and a face brightness multiplier (vertex colors or equivalent attribute). Preserve the existing face-culling rules and one-geometry-per-chunk pipeline.
  </action>
</task>

<task type="auto">
  <name>Task 3: Wire shared textured material into the game loop</name>
  <files>src/rendering/ChunkMaterial.ts, src/core/Game.ts</files>
  <action>
    Replace the Phase 1 flat green material with a shared textured material configured for nearest filtering, correct color space, and front-face opaque rendering. Ensure chunk remesh/dispose lifecycle still works unchanged.
  </action>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- `src/meshing/CulledMesher.ts` still skips hidden faces between adjacent solid blocks
- Browser check: grass, dirt, stone, and sand are visually distinguishable; top faces read brighter than side faces
</verification>

<output>
Create `.planning/phases/02-world-player/02-01-SUMMARY.md` with atlas layout, block-face mapping, shading approach, and any texture-bleed safeguards used.
</output>
