---
phase: 02-world-player
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
  - 02-01
  - 02-03
files_modified:
  - src/world/World.ts
  - src/core/Renderer.ts
  - src/core/Game.ts
autonomous: false
requirements:
  - TERR-04
  - TERR-05
  - PLYR-01
  - PLYR-02
  - PLYR-03
  - PLYR-04
  - PLYR-05

must_haves:
  truths:
    - "Chunks are requested in player-relative distance order so nearby terrain loads first while moving"
    - "Chunks beyond the unload radius are removed from the scene and their geometry is disposed immediately"
    - "Fog hides the render boundary so missing far-edge chunks are not visible as hard seams"
    - "The player can continuously explore away from the origin without the world staying fixed around (0,0)"
    - "Chunk mesh application is budgeted/paced enough to avoid obvious frame hitches while moving"
  artifacts:
    - path: "src/world/World.ts"
      provides: "Player-centered desired chunk set, load priority, and unload bookkeeping"
    - path: "src/core/Renderer.ts"
      provides: "Scene fog configured to match render distance"
    - path: "src/core/Game.ts"
      provides: "Integration of player position, world streaming, and chunk mesh disposal"
  key_links:
    - from: "src/core/Game.ts"
      to: "src/world/World.ts"
      via: "player chunk position drives desired chunk window each frame"
      pattern: "updateStreaming|setCenter|ensureChunks"
    - from: "src/core/Game.ts"
      to: "src/core/Renderer.ts"
      via: "fog/render distance stays aligned with streaming radius"
      pattern: "fog|renderDistance"
---

<objective>
Finish Phase 2 by replacing the origin-locked demo loop with player-centered chunk streaming, unload/dispose lifecycle, and fog that conceals the world boundary during exploration.
</objective>

<context>
@.planning/phases/02-world-player/02-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add player-centered streaming and unload lifecycle</name>
  <files>src/world/World.ts, src/core/Game.ts</files>
  <action>
    Replace the fixed origin-centered 5x5 request loop with a player-relative chunk window, request nearest chunks first, and identify chunks that should unload when outside the retention radius.
  </action>
</task>

<task type="auto">
  <name>Task 2: Align fog and render pacing with streaming radius</name>
  <files>src/core/Renderer.ts, src/core/Game.ts</files>
  <action>
    Configure scene fog to hide the chunk boundary and budget chunk mesh rebuild/application work so terrain appears progressively rather than in hard bursts.
  </action>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify exploration loop in browser</name>
  <action>Run `npm run dev`, click to lock, walk away from spawn, and confirm streaming/fog/player behavior match the Phase 2 success criteria.</action>
  <resume-signal>
    Type "approved" if exploration feels correct, or describe the visible issue (e.g. chunk pop-in, visible edge, falling through terrain, no unload).
  </resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- Browser check: move several chunks away from spawn; new chunks appear ahead, old chunks unload behind, fog conceals the edge, and no console errors appear
</verification>

<success_criteria>
Phase 2 is complete when all success criteria from `ROADMAP.md` are true:
1. Textured block faces are clearly identifiable
2. Walking toward the horizon loads new chunks progressively without hard pop-in
3. Fog hides the render-distance edge
4. Mouse look, WASD, gravity, collision, and jump all work
5. Hidden faces stay culled and directional shading remains visible
</success_criteria>

<output>
Create `.planning/phases/02-world-player/02-04-SUMMARY.md` with streaming radius, unload policy, fog settings, browser verification notes, and any pacing limits added.
</output>
