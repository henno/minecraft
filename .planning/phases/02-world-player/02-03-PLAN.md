---
phase: 02-world-player
plan: 03
type: execute
wave: 3
depends_on:
  - 01-03
  - 02-02
files_modified:
  - src/player/PlayerPhysics.ts
  - src/player/Player.ts
  - src/world/World.ts
  - src/core/Game.ts
autonomous: true
requirements:
  - PLYR-03
  - PLYR-04
  - PLYR-05

must_haves:
  truths:
    - "The player falls under gravity when not supported by terrain"
    - "The player collides with solid voxel blocks and cannot move through them"
    - "Jumping works only from grounded state and produces a visible upward impulse"
    - "Collision queries use `World.getBlock()` / world-level solidity rules, not direct chunk data access"
    - "The player spawns at a sane position near the terrain surface so the phase is immediately testable"
  artifacts:
    - path: "src/player/PlayerPhysics.ts"
      provides: "AABB gravity, movement, and collision resolution against voxel terrain"
    - path: "src/player/Player.ts"
      provides: "Input-to-physics bridge and player transform state"
  key_links:
    - from: "src/player/PlayerPhysics.ts"
      to: "src/world/World.ts"
      via: "solid block queries during movement resolution"
      pattern: "getBlock"
    - from: "src/player/Player.ts"
      to: "src/player/PlayerPhysics.ts"
      via: "per-frame physics update"
      pattern: "PlayerPhysics"
---

<objective>
Make the player physically belong to the world: apply gravity, resolve collisions against voxel terrain, and allow jumping from grounded terrain.
</objective>

<context>
@.planning/phases/02-world-player/02-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add voxel collision physics</name>
  <files>src/player/PlayerPhysics.ts</files>
  <action>
    Implement an axis-separated AABB resolver that checks solid voxels through `World.getBlock()`, applies gravity each frame, and reports grounded state.
  </action>
</task>

<task type="auto">
  <name>Task 2: Integrate movement intent, grounded jump, and spawn logic</name>
  <files>src/player/Player.ts, src/core/Game.ts, src/world/World.ts</files>
  <action>
    Feed camera-relative movement intent into physics, add jump handling, and choose a reliable spawn near origin terrain. Any world helper added here should stay world-owned and deterministic.
  </action>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- Browser check: player falls off edges, lands on terrain, cannot walk through hills, and can jump while grounded
</verification>

<output>
Create `.planning/phases/02-world-player/02-03-SUMMARY.md` with collider dimensions, gravity/jump constants, spawn strategy, and known edge cases.
</output>
